/**
 * Nex Event API
 * Utilize essa documentação para realizar a integração com o nosso sistema
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PaginationResponse model module.
 * @module model/PaginationResponse
 * @version 1.0
 */
class PaginationResponse {
    /**
     * Constructs a new <code>PaginationResponse</code>.
     * @alias module:model/PaginationResponse
     * @param totalItems {Number} 
     * @param totalPages {Number} 
     * @param remainingPages {Number} 
     * @param nextPage {Number} 
     * @param prevPage {Number} 
     */
    constructor(totalItems, totalPages, remainingPages, nextPage, prevPage) { 
        
        PaginationResponse.initialize(this, totalItems, totalPages, remainingPages, nextPage, prevPage);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, totalItems, totalPages, remainingPages, nextPage, prevPage) { 
        obj['totalItems'] = totalItems;
        obj['totalPages'] = totalPages;
        obj['remainingPages'] = remainingPages;
        obj['nextPage'] = nextPage;
        obj['prevPage'] = prevPage;
    }

    /**
     * Constructs a <code>PaginationResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PaginationResponse} obj Optional instance to populate.
     * @return {module:model/PaginationResponse} The populated <code>PaginationResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PaginationResponse();

            if (data.hasOwnProperty('totalItems')) {
                obj['totalItems'] = ApiClient.convertToType(data['totalItems'], 'Number');
            }
            if (data.hasOwnProperty('totalPages')) {
                obj['totalPages'] = ApiClient.convertToType(data['totalPages'], 'Number');
            }
            if (data.hasOwnProperty('remainingPages')) {
                obj['remainingPages'] = ApiClient.convertToType(data['remainingPages'], 'Number');
            }
            if (data.hasOwnProperty('nextPage')) {
                obj['nextPage'] = ApiClient.convertToType(data['nextPage'], 'Number');
            }
            if (data.hasOwnProperty('prevPage')) {
                obj['prevPage'] = ApiClient.convertToType(data['prevPage'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PaginationResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PaginationResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PaginationResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }

        return true;
    }


}

PaginationResponse.RequiredProperties = ["totalItems", "totalPages", "remainingPages", "nextPage", "prevPage"];

/**
 * @member {Number} totalItems
 */
PaginationResponse.prototype['totalItems'] = undefined;

/**
 * @member {Number} totalPages
 */
PaginationResponse.prototype['totalPages'] = undefined;

/**
 * @member {Number} remainingPages
 */
PaginationResponse.prototype['remainingPages'] = undefined;

/**
 * @member {Number} nextPage
 */
PaginationResponse.prototype['nextPage'] = undefined;

/**
 * @member {Number} prevPage
 */
PaginationResponse.prototype['prevPage'] = undefined;






export default PaginationResponse;

