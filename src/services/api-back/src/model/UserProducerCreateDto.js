/**
 * Nex Event API
 * Utilize essa documentação para realizar a integração com o nosso sistema
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The UserProducerCreateDto model module.
 * @module model/UserProducerCreateDto
 * @version 1.0
 */
class UserProducerCreateDto {
    /**
     * Constructs a new <code>UserProducerCreateDto</code>.
     * @alias module:model/UserProducerCreateDto
     * @param email {String} User email
     * @param password {String} User password
     * @param confirmPassword {String} User confirm password
     */
    constructor(email, password, confirmPassword) { 
        
        UserProducerCreateDto.initialize(this, email, password, confirmPassword);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, email, password, confirmPassword) { 
        obj['email'] = email;
        obj['password'] = password;
        obj['confirmPassword'] = confirmPassword;
    }

    /**
     * Constructs a <code>UserProducerCreateDto</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UserProducerCreateDto} obj Optional instance to populate.
     * @return {module:model/UserProducerCreateDto} The populated <code>UserProducerCreateDto</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UserProducerCreateDto();

            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], 'String');
            }
            if (data.hasOwnProperty('password')) {
                obj['password'] = ApiClient.convertToType(data['password'], 'String');
            }
            if (data.hasOwnProperty('confirmPassword')) {
                obj['confirmPassword'] = ApiClient.convertToType(data['confirmPassword'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UserProducerCreateDto</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UserProducerCreateDto</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of UserProducerCreateDto.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['email'] && !(typeof data['email'] === 'string' || data['email'] instanceof String)) {
            throw new Error("Expected the field `email` to be a primitive type in the JSON string but got " + data['email']);
        }
        // ensure the json data is a string
        if (data['password'] && !(typeof data['password'] === 'string' || data['password'] instanceof String)) {
            throw new Error("Expected the field `password` to be a primitive type in the JSON string but got " + data['password']);
        }
        // ensure the json data is a string
        if (data['confirmPassword'] && !(typeof data['confirmPassword'] === 'string' || data['confirmPassword'] instanceof String)) {
            throw new Error("Expected the field `confirmPassword` to be a primitive type in the JSON string but got " + data['confirmPassword']);
        }

        return true;
    }


}

UserProducerCreateDto.RequiredProperties = ["email", "password", "confirmPassword"];

/**
 * User email
 * @member {String} email
 */
UserProducerCreateDto.prototype['email'] = undefined;

/**
 * User password
 * @member {String} password
 */
UserProducerCreateDto.prototype['password'] = undefined;

/**
 * User confirm password
 * @member {String} confirmPassword
 */
UserProducerCreateDto.prototype['confirmPassword'] = undefined;






export default UserProducerCreateDto;

